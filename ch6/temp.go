package main

func main() {
	//var x, y practice.IntSet

	/*	perim := geometry.Path{
			{1, 1},
			{5, 1},
			{5, 4},
			{1, 1},
		}
		fmt.Println(perim.Distance())
		fmt.Println(geometry.PathDistance(perim))
		r := &geometry.Point{1, 2}
		r.ScaleBy(2)
		fmt.Println(*r) // "{2, 4}"
		t := geometry.Point{
			2,
			2,
		}
		tt := &t
		tt.ScaleBy(2)
		fmt.Println(*tt)
		t.ScaleBy(2)
		fmt.Println(*tt)*/
	//t := geometry.Point{
	//	1,
	//	2,
	//}
	//tt := &t
	//fmt.Println(tt.Distance(geometry.Point{2, 2}))
}
